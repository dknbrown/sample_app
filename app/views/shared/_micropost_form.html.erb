<%= form_for @micropost do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
    <%= f.text_area :content %>
    <p><span id="char_count">&nbsp; </span></p>
  </div>
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
  
<% end %>
